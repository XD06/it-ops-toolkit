# 路线图

## 路线图原则

路线图不是功能愿望清单，而是控制项目节奏的工具。

本项目要避免两个极端：

- 一开始只写零散脚本，后面无法扩展。
- 一开始就做大平台，长期没有可用成果。

正确路线是：先建立平台骨架，再通过一个个可运行模块逐步扩展。

## Phase 0：文档与架构基础

目标：在写代码前建立项目记忆。

交付物：

- 项目愿景。
- 总开发与架构文档。
- 模块地图。
- 开发规则。
- 术语表。
- 调研依据。
- 第一条 ADR。
- 初始模块文档。

验收标准：

- 能清楚说明第一阶段做什么，不做什么。
- 能清楚说明模块边界。
- 能清楚说明 CLI、Web、AI、Agent 的关系。
- 后续开发某模块时，不需要重新讨论整个大方向。

## Phase 1：CLI 基础与最小运维闭环

目标：做出一个实际可用的 CLI 工具箱。

交付物：

- CLI 入口。
- 配置中心。
- 基础 Adapter：Ping、DNS、TCP、HTTP。
- 资产发现最小流程。
- 网络与服务巡检最小流程。
- 结构化结果模型。
- 本地数据存储。
- Markdown 或 HTML 报告。
- CSV 导出。

详细实施顺序见 `docs/plans/phase-1-cli-foundation.md`。

验收标准：

- 用户能配置一个网段或目标列表。
- 用户能运行资产发现命令。
- 用户能运行巡检命令。
- 系统能保存结构化结果。
- 系统能生成可读报告。
- CLI 不包含核心业务逻辑。
- 未来 Web 可以复用同一批服务。

## Phase 2：场景化故障诊断

目标：把单项检查变成排障流程。

交付物：

- 诊断工作流格式。
- 常见诊断场景：
  - 上不了网。
  - 内网系统打不开。
  - 远程桌面连不上。
  - DNS 异常。
  - 网络很慢。
  - 打印机不可达。
- 诊断报告。
- 证据链输出。

验收标准：

- 用户能选择一个故障场景。
- 系统能自动执行相关检查。
- 系统能输出可能原因、证据和下一步建议。

## Phase 3：轻量安全与自动化动作

目标：覆盖中小企业最常见的基础风险，并标准化低风险动作。

交付物：

- 高风险端口检查。
- 未知设备检测。
- 新开放端口检测。
- 证书过期检查。
- 基础安全状态检查。
- 自动化动作风险分级。
- 只读和低风险动作。

验收标准：

- 系统能输出风险清单。
- 自动化动作都有风险等级。
- 高风险动作不会被默认自动执行。

## Phase 4：Web Console MVP

目标：在不重写核心逻辑的前提下增加 Web 可视化。

交付物：

- 资产列表。
- 资产详情。
- 任务历史。
- 巡检结果页面。
- 报告查看。
- 配置查看或编辑。
- 手动任务触发。

验收标准：

- Web 调用已有应用服务。
- Web 不直接调用 Adapter。
- Web 能查看 CLI 产生的历史结果。

## Phase 5：AI 运维助手

目标：让 AI 基于结构化数据做解释、总结和建议。

交付物：

- AI 输入数据结构。
- 巡检报告总结。
- 异常结果解释。
- 排障建议生成。
- 周报草稿生成。
- SOP 匹配。

验收标准：

- AI 能引用结构化结果。
- AI 输出能区分事实、推断和建议。
- AI 不默认执行高风险动作。

## Phase 6：受控 Agent 工作流

目标：让 Agent 在审批和审计保护下执行预定义流程。

交付物：

- 工作流定义。
- 风险分类。
- 权限检查。
- 审批点。
- 执行日志。
- 停止条件。
- 人工接管机制。

验收标准：

- Agent 可以执行只读诊断流程。
- 涉及风险动作时必须请求审批。
- 每一步执行都有审计记录。

## 当前推荐下一步

Phase 0 已经开始。下一步建议：

1. Review 总开发与架构文档。
2. Review 模块地图。
3. 明确 Phase 1 的技术栈。
4. 为配置中心和 CLI 基础写具体模块实现计划。
5. 开始实现最小 CLI 闭环。
